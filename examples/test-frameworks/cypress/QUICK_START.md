# Швидкий старт - Перевірка прикладів

## ✅ Перевірка синтаксису

Всі приклади коду перевірено на синтаксичні помилки:

```bash
cd examples/test-frameworks/cypress
./check-examples.sh
```

**Результат:** ✅ Всі файли мають правильний синтаксис

## 🌐 Перевірка підключення до API

Перевірте, чи працюють реальні API:

```bash
node test-api-connection.js
```

**Очікуваний результат:**
- ✅ JSONPlaceholder працює
- ✅ ReqRes працює  
- ✅ httpbin.org працює

## 🧪 Тестування прикладів

### 1. Console приклади

**Відкрийте браузер та виконайте:**

1. Відкрийте https://example.com
2. Натисніть F12 → Console
3. Скопіюйте та виконайте код з `docs/03-devtools-console/examples/console-examples.js`

**Приклад:**
```javascript
// Тестування селекторів
const h1 = document.querySelector('h1');
console.log('h1 found:', h1);
console.log('h1 text:', h1?.textContent);
```

### 2. Network приклади

**Відкрийте браузер та виконайте:**

1. Відкрийте https://example.com
2. Натисніть F12 → Console
3. Скопіюйте код перехоплювача з `docs/01-devtools-network/examples/console-examples.js`
4. Виконайте реальний запит:

```javascript
// Спочатку активуйте перехоплювач (скопіюйте код з файлу)
// Потім виконайте:
fetch('https://jsonplaceholder.typicode.com/users/1')
    .then(r => r.json())
    .then(console.log);
```

### 3. Cypress тести

**Запуск БЕЗ встановлення (рекомендовано):**

```bash
cd examples/test-frameworks/cypress

# Швидкий тест
npx --yes cypress@latest run --spec "cypress/e2e/quick-test.cy.js" --headless

# Всі тести
npx --yes cypress@latest run --headless

# Інтерактивний режим
npx --yes cypress@latest open
```

**Або встановити локально:**

```bash
cd examples/test-frameworks/cypress
npm install
npm run cypress:open
```

## 📋 Чеклист перевірки

- [ ] Синтаксис JavaScript файлів правильний
- [ ] API підключення працює (JSONPlaceholder, ReqRes, httpbin)
- [ ] Console приклади працюють на example.com
- [ ] Cypress тести можна запустити
- [ ] Демо-сторінка відкривається в браузері

## 🐛 Якщо щось не працює

1. **API не відповідає:**
   - Перевірте інтернет-з'єднання
   - Спробуйте відкрити API в браузері вручну

2. **Console приклади не працюють:**
   - Переконайтеся, що ви на правильному сайті (example.com)
   - Перевірте, чи DevTools відкрито

3. **Cypress тести не запускаються:**
   - Перевірте, чи встановлено Cypress: `npm install`
   - Перевірте версію Node.js (потрібна 18+)

## 💡 Поради

- Всі приклади готові до використання
- Почніть з Console прикладів - вони найпростіші
- Network приклади потребують активації перехоплювачів перед виконанням запитів
- Cypress тести можна запускати окремо для кожної теми

